/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  trailingSlash: true,
  // If you host under a repository path (USERNAME.github.io/REPO), set basePath and assetPrefix
  // basePath: '/REPO-NAME',
  // assetPrefix: '/REPO-NAME/',
};

module.exports = nextConfig;
